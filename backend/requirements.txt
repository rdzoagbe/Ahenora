fastapi==0.110.0
uvicorn==0.27.1
cryptography>=42.0.8  # hard dependency of google-auth (id-token verification)
pymongo==4.6.3  # transitive via motor; pinned deliberately so deploys don't drift
pydantic>=2.13.4
tzdata>=2024.2
motor==3.3.1
pytest>=9.0.3
black>=26.5.1
isort>=5.13.2
flake8>=7.0.0
mypy>=2.1.0
requests>=2.31.0
pandas>=2.2.0
numpy>=2.4.6  # transitive via pandas; floor pinned deliberately
python-multipart>=0.0.9  # no import anywhere; FastAPI needs it for multipart uploads
google-genai>=2.14.0,<3
pillow>=12.2.0
google-auth>=2.29.0
openpyxl>=3.1.2  # looks unused, is not: pd.read_excel(engine="openpyxl") passes it as a string
python-docx>=1.1.2
