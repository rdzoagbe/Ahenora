fastapi==0.141.1
uvicorn==0.52.3
cryptography>=50.0.0  # hard dependency of google-auth (id-token verification)
pymongo==4.17.0  # transitive via motor; pinned deliberately so deploys don't drift
pydantic>=2.13.4
tzdata>=2026.3
motor==3.7.1
pytest>=9.1.1
black>=26.5.1
isort>=8.0.1
flake8>=7.3.0
mypy>=2.3.0
requests>=2.34.2
pandas>=3.0.5,<4  # the unbounded floor was already resolving 3.x on every deploy; now it says so, bounded against the next major
numpy>=2.4.6  # transitive via pandas; floor pinned deliberately
python-multipart>=0.0.32  # no import anywhere; FastAPI needs it for multipart uploads
google-genai>=2.18.1,<3
pillow>=12.3.0
google-auth>=2.56.3
openpyxl>=3.1.5  # looks unused, is not: pd.read_excel(engine="openpyxl") passes it as a string
python-docx>=1.2.0
